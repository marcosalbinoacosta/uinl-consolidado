name: Refrescar insights

on:
  schedule:
    # 10:00 UTC = 06:00 Bolivia (UTC-4) — captura notas de la noche anterior
    - cron: "0 10 * * *"
    # 22:00 UTC = 18:00 Bolivia (UTC-4) — captura el día completo de actividad
    - cron: "0 22 * * *"
  # Trigger manual desde Actions → "Run workflow"
  workflow_dispatch:

jobs:
  extract:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run extract
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: npm run extract

      - name: Commit y push si hay cambios
        run: |
          if git diff --quiet data/insights.json; then
            echo "Sin cambios en data/insights.json — nada que commitear."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/insights.json
          git commit -m "data: auto-refresh insights ($(date -u +'%Y-%m-%d %H:%M UTC'))"
          git push
